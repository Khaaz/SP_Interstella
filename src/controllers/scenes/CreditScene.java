package controllers.scenes;

import controllers.AController;
import javafx.scene.Parent;

public class CreditScene extends AScene {
    public CreditScene(Parent root, AController controller) {
        super(root, controller);
    }
}
